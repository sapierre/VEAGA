import { Main } from "~/components/common/main";
import { Layout } from "~/components/layout/layout";

const Options = () => {
  return (
    <Layout className="p-8">
      <Main filename="src/app/options" />
    </Layout>
  );
};

export default Options;
